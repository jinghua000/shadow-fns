/**
 * Always return `false`
 * 
 * @param {*} _ 
 * @return {Boolean}
 * @since 0.1.0
 * @category Logic
 * @sign x -> Boolean
 * @see T
 * @example
 * 
 * f.F() // => fasle
 * f.F(10) // => fasle
 * f.F(null) // => fasle
 */
const F = _ => false

export default F