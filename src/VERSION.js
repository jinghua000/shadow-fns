import pkg from '../package.json'

/**
 * Library's version
 * 
 * @type {String}
 * @category Other
 * @since 0.1.4
 * @example
 * 
 * f.VERSION // => 0.1.4
 */
const VERSION = pkg.version

export default VERSION