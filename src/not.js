/**
 * Return opposite boolean of the parameter supplied
 * 
 * @param {*} e 
 * @return {Boolean}
 * @since 0.1.0
 * @category Logic
 * @sign x -> Boolean
 * @see opposite
 * @example
 * 
 * f.not(true) // => false
 * f.not(null) // => true
 */
const not = e => !e

export default not