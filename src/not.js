/**
 * Return opposite boolean of the parameter supplied
 * 
 * @param {*} e 
 * @return {Boolean}
 * @category Logic
 * @sign x -> Boolean
 * @see opposite
 * @example
 * 
 * f.not(true) // => false
 * f.not(null) // => true
 */
const not = e => !e

export default not