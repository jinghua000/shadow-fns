/**
 * Always return `true`
 * 
 * @param {*} _ 
 * @return {Boolean}
 * @category Logic
 * @sign x -> Boolean
 * @see F
 * @example
 * 
 * f.T() // => true
 * f.T(10) // => true
 * f.T(null) // => true
 */
const T = _ => true

export default T