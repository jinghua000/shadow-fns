/**
 * Always return `true`
 * 
 * @param {*} _ 
 * @return {Boolean}
 * @since 0.1.0
 * @category Logic
 * @sign x -> Boolean
 * @see F
 * @example
 * 
 * f.T() // => true
 * f.T(10) // => true
 * f.T(null) // => true
 */
const T = _ => true

export default T